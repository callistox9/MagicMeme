import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

const Render =ReactDOM.createRoot(document.getElementById('root')); // A variable to render App Component.

Render.render(
  
    <App />
  
)
